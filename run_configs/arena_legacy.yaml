# Public legacy arena template.
# Project-specific empirical context, identifiers, codebooks and storage paths
# from earlier institutional research are intentionally not published.

topic: legacy
country: null
language: null

analysis:
  laclau: true
  palonen: true
  sentiment: true
  topics: true
  entities: true
  context_memory: true
  temporal: true

codebook: null
seed_codebook_mode: none

runtime:
  data_root: ${LACLAUGPT_DATA_DIR:-./data}
