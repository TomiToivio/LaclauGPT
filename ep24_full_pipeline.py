# -*- coding: utf-8 -*-
"""Deprecated compatibility shim for a removed institutional full-corpus pipeline.

Project-specific country mappings, manifests, storage layout, scheduler settings
and empirical codebooks are not published. Use the canonical LaclauGPT pipeline
with `legacy` configuration supplied through an authorized research environment.
"""

raise RuntimeError(
    "This project-specific full-corpus adapter has been retired from the public "
    "repository. Use the canonical pipeline with authorized legacy configuration."
)
